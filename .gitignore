node_modules
.next
dist
.env
.env.local
*.tsbuildinfo
.lighthouse-secrets.json
.lighthouseci
lighthouse-ci-artifacts

# OSM / Geofabrik runtime caches (Overpass tiles, PBF downloads, geojson uploads)
.cache/

# Tooling / deploy
.vercel
coverage

# OS junk
.DS_Store
Thumbs.db

# Logs
*.log

# Gossip lab node-c keys are generated locally/CI (not committed — GitGuardian)
docker/gossip-lab/node-c.private.pem
docker/gossip-lab/node-c.public.pem
